# 📘 АУДИТ: nodeStore.ts

## 1. Глобальное Описание

### Назначение
`nodeStore` — это основное хранилище состояния для модульного редактора **The Grid**, построенное на базе `Zustand`. Оно управляет списком узлов, связями между ними и их внутренними параметрами. Это "мозг" системы, который связывает визуальный интерфейс ReactFlow с аудио-процессором GraphEngine.

### Ключевые структуры
- **NODE_DEFS:** Манифест всех доступных типов узлов с их портами ввода/вывода, категориями и дефолтными параметрами.
- **NodeType:** Строго типизированный список всех идентификаторов модулей.

---

## 2. Логика хранилища

### addNode
Метод создания нового узла. При создании автоматически внедряет "стандартные параметры" DSP:
- `inputGain`: 1.0
- `outputGain`: 1.0
- `bypass`: false
Это гарантирует предсказуемое поведение всех аудио-процессоров.

---

## 3. Genesis Log

---
## [ИСТОРИЯ ИЗМЕНЕНИЙ]
**Дата аудита:** 2026-02-02 04:54:00
**Событие:** ПЕРВИЧНЫЙ ГЛУБОКИЙ АУДИТ (GENESIS)

**Детализация:**
- Описана структура хранения узлов и связей.
- Манифест \`NODE_DEFS\` расширен до 50+ модулей, включая логику, секвенсоры и продвинутые FX.
- Реализована централизованная инъекция параметров усиления.
- Добавлена поддержка JSFX скриптов через поле \`script\`.
- **[UPDATE 2026-02-02 21:55]**: Интеграция WASM DSP Sandbox. Добавлен тип узла `wasm_node` для работы с внешними AudioWorklets и бинарными плагинами.

### Глубокий разбор логики и математики
- **Логика:** Хранилище использует \`applyNodeChanges\` и \`applyEdgeChanges\` из ReactFlow для атомарного обновления состояния графа.
- **Математика:** Идентификаторы генерируются через `crypto.randomUUID()`, обеспечивая уникальность каждого блока в графе.

---
## [UPDATE LOG] [2026-02-02] [22:35:00]
**Триггер:** Реализация системы Macro Controls (Макро-регуляторы).
**Измененный элемент:** Store: `nodeStore.ts`

### Детали изменений (Diff Analysis)
- **[БЫЛО]:** Хранилище управляло только списком узлов и их локальными параметрами. Глобальное управление отсутствовало.
- **[СТАЛО]:** Добавлена система из 8 глобальных макросов, способных управлять любым параметром любой ноды. Реализована логика маппинга и интерполяции.
- **[ТЕХНИЧЕСКИЕ ИЗМЕНЕНИЯ]:**
  - [ADDED] `MacroKnob`: Интерфейс для хранения состояния макроса (ID, label, value, target, range).
  - [ADDED] `macros`: Массив из 8 макросов в состоянии хранилища.
  - [ADDED] `setMacroValue(id, value)`: Метод обновления значения макроса с автоматическим пробросом в целевой параметр.
  - [ADDED] `assignMacro(id, nodeId, param, range)`: Метод привязки макроса к конкретной ноде.
  - [MODIFIED] `saveDevice`, `loadDevice`: Обновлены для поддержки сериализации состояния макросов.

### Глубокий разбор логики и математики
- **Логика:** Система поддерживает "one-to-many" через `assignMacro`. При изменении значения макроса (0..1), оно масштабируется под диапазон (range) целевого параметра.
- **Математика:** Поддерживается два типа интерполяции:
  1. **Линейная ($$Linear$$):**
     $$Value_{target} = Min + MacroValue \times (Max - Min)$$
  2. **Логарифмическая ($$Log$$ - для частот):**
     $$Value_{target} = \exp(\ln(Min) + MacroValue \times (\ln(Max) - \ln(Min)))$$
EOF
